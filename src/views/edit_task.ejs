<%- include ('partials/header') -%>
<nav class="navbar navbar-dark bg-dark">
    <a href="/" class="navbar-brand">Crud nodemonbo</a>
</nav>
<!--
    <div class="row m-4">
        <div class="col-md-5 offset-md-3">
            <div class="card">
                <div class="card-body">
                    <form action="/edit/<%= task.id %> " method="POST">
                        <div class="form-group">
                            <input class="form-control" type="text" name="title" placeholder="insert a Title"
                                value="<%= task.title %> ">
                        </div>
                        <div class="form-group">
                            <textarea class="form-control" name="description" cols="80"
                                placeholder="insert a description" /><%= task.description %></textarea>
                        </div>
                        <div class="form-group d-grid gap-2">
                            <button type="submit" class="btn btn-primary ">Add</button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
   -->
    <%- include ('partials/footer') -%>